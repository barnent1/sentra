/**
 * Returns a farewell message for the given name
 * @param name - The name to include in the farewell message
 * @returns A farewell message string
 */
export function farewell(name: string): string {
  return `Goodbye, ${name}! See you soon.`
}
