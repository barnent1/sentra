name: Sentra AI Agent Worker

# Triggers when issue is labeled with "ai-feature"
on:
  issues:
    types: [labeled]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to work on'
        required: true
        type: number

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  ai-agent-work:
    # Only run if issue has "ai-feature" label
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'ai-feature')) ||
      (github.event_name == 'issue_comment' && contains(github.event.issue.labels.*.name, 'ai-feature'))

    runs-on: ubuntu-latest
    timeout-minutes: 45  # Higher limit for complex Sentra tasks

    # Phase 1: Docker Containerization for Security
    # See: ADR_0001_CONTAINER_SECURITY.md
    container:
      image: ghcr.io/${{ github.repository_owner }}/sentra-agent:latest
      options: >-
        --cap-drop=ALL
        --security-opt=no-new-privileges
        --read-only
        --tmpfs /tmp:exec,mode=1777
        --tmpfs /home/claude-agent/.cache:exec,mode=0755
        --tmpfs /home/claude-agent/workspace:exec,mode=0755
        --memory=2g
        --memory-swap=2g
        --cpus=2
        --pids-limit=100
        --oom-kill-disable=false
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for context

      # Container already has Python, Node.js, and npm installed
      # See: .claude/Dockerfile for complete environment setup

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install npm dependencies
        run: npm ci
        working-directory: ${{ github.workspace }}

      - name: Verify container environment
        run: |
          echo "🐳 Running in secure Docker container"
          echo "Python: $(python3.11 --version)"
          echo "Node: $(node --version)"
          echo "npm: $(npm --version)"
          echo "Next.js: $(npx next --version 2>&1 || echo 'NOT FOUND')"
          echo "User: $(whoami)"
          echo "UID: $(id -u)"
          echo "Claude Code: $(claude --version 2>&1 || echo 'NOT INSTALLED')"
          echo "PATH: $PATH"

      - name: Create feature branch
        run: |
          ISSUE_NUMBER=${{ github.event.inputs.issue_number || github.event.issue.number }}
          BRANCH_NAME="feature/issue-${ISSUE_NUMBER}"

          # Check if branch already exists
          if git ls-remote --heads origin "$BRANCH_NAME" | grep -q "$BRANCH_NAME"; then
            echo "Branch already exists, checking out"
            git fetch origin "$BRANCH_NAME"
            git checkout "$BRANCH_NAME"
          else
            echo "Creating new branch"
            git checkout -b "$BRANCH_NAME"
          fi

          # Configure git
          git config user.name "Sentra AI Agent"
          git config user.email "agent@sentra.dev"

      - name: Load Sentra project context
        run: |
          echo "📚 Loading Sentra project context..."
          if [ -f ".sentra/memory/project-overview.md" ]; then
            echo "✅ Project overview loaded"
          fi
          if [ -f ".sentra/config.yml" ]; then
            echo "✅ Project configuration loaded"
          fi

      - name: Debug Python environment
        run: |
          echo "=== Python Environment Debug ==="
          echo "Python version:"
          python3.11 --version
          echo "Python path:"
          python3.11 -c "import sys; print('\n'.join(sys.path))"
          echo "PYTHONPATH: $PYTHONPATH"
          echo "Checking for anthropic package:"
          python3.11 -c "import anthropic; print(f'anthropic version: {anthropic.__version__}')" || echo "anthropic not found"
          echo "User site-packages:"
          ls -la /home/claude-agent/.local/lib/python3.11/site-packages/ | head -20

      - name: Run AI Agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Claude Code CLI configuration
          DISABLE_AUTOUPDATER: 'true'
          DISABLE_TELEMETRY: 'true'
          # Agent constraints
          CLAUDE_MAX_EXECUTION_TIME: '45'
          CLAUDE_MAX_API_CALLS_PER_ISSUE: '150'
          CLAUDE_MAX_FILE_CHANGES: '75'
          CLAUDE_REQUIRE_TESTS: 'false'  # No tests yet in early phase
          CLAUDE_GITHUB_COMMENTS: 'true'
          CLAUDE_LOG_API_CALLS: 'true'
          # Rate limiting configuration (prevent 429 errors)
          CLAUDE_RATE_LIMIT_TPM: '25000'  # 25k tokens/min (under 30k limit)
          CLAUDE_RATE_LIMIT_RETRIES: '3'  # Retry 3 times with backoff
          CLAUDE_RATE_LIMIT_THRESHOLD: '0.8'  # Throttle at 80% of limit
        run: |
          ISSUE_NUMBER=${{ github.event.inputs.issue_number || github.event.issue.number }}

          # Scripts are checked into the repository at .claude/scripts/
          chmod +x .claude/scripts/ai-agent-worker.py .claude/scripts/notify.sh

          # Run the AI agent (using python3.11 to match Dockerfile)
          python3.11 .claude/scripts/ai-agent-worker.py "$ISSUE_NUMBER" . | tee agent-output.json || exit 1

      - name: Upload agent logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agent-logs-${{ github.event.issue.number }}
          path: |
            ~/.claude/telemetry/agents.log
            agent-output.json
          retention-days: 30

      - name: Report success
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ISSUE_NUMBER=${{ github.event.inputs.issue_number || github.event.issue.number }}
          WORKFLOW_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          BODY="✅ AI agent completed this task successfully!\n\nA pull request should be created shortly. Please review the changes and merge when ready.\n\nView [workflow logs]($WORKFLOW_URL) for details."
          gh issue comment "$ISSUE_NUMBER" --body "$BODY"

      - name: Report failure
        if: failure()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ISSUE_NUMBER=${{ github.event.inputs.issue_number || github.event.issue.number }}
          WORKFLOW_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          BODY="❌ AI agent encountered an issue while working on this task.\n\nCheck the [workflow logs]($WORKFLOW_URL) for details.\n\nThis issue has been labeled 'needs-help' and requires human attention."
          gh issue comment "$ISSUE_NUMBER" --body "$BODY"
          gh issue edit "$ISSUE_NUMBER" --add-label "needs-help"
